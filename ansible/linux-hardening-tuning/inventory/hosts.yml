all:
    children:
        web-servers:
            hosts:
                hetzner:
                    ansible_host: ip
                    ansible_user: root
                    ansible_port: 22

                sharif:
                    ansible_host: ip
                    ansible_user: root
                    ansible_port: 22